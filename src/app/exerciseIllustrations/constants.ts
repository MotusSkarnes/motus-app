/** Filstørrelse ved opplasting/generering (kvadrat 1:1). */
export const EXERCISE_ILLUSTRATION_PIXEL_SIZE = 512;

/** Liten thumbnail i lister (40×40 px). */
export const EXERCISE_IMAGE_THUMB_CLASS =
  "h-10 w-10 max-h-10 max-w-10 shrink-0 rounded-lg border object-cover bg-white";

/** Medium i øktmodus / forhåndsvisning (80×80 px). */
export const EXERCISE_IMAGE_MEDIUM_CLASS =
  "h-20 w-20 max-h-20 max-w-20 shrink-0 rounded-xl border object-cover bg-white";

/** Liten forhåndsvisning neste øvelse (56×56 px). */
export const EXERCISE_IMAGE_SMALL_CLASS =
  "h-14 w-14 max-h-14 max-w-14 shrink-0 rounded-lg border object-cover bg-white";

/** Inni fast kvadratisk ramme (f.eks. knapp med h-20 w-20). */
export const EXERCISE_IMAGE_INSET_CLASS = "block h-full w-full max-h-full max-w-full object-cover";
